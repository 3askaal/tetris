import React from 'react';
import { render } from '../../tests';
import PlayView from './PlayView';

describe('<PlayView />', () => {
  it('renders component', () => {
    render(<PlayView />)
  });
})
