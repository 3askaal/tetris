import React from 'react';
import { render } from '../../tests';
import { Controls } from './Controls';

describe('<Controls />', () => {
  it('renders component', () => {
    render(<Controls />)
  });
})
